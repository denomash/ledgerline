# Shared local-dev defaults for docker-compose services.
# Copy to .env and adjust as needed. App-specific vars live in apps/*/.env.example.

POSTGRES_USER=example
POSTGRES_PASSWORD=example
POSTGRES_DB=example
