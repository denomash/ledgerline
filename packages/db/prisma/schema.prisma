generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Models land in Phase 1 (User, Group, Membership) and Phase 2 (Platform, LedgerEntry, Holding).
